<!--
SPDX-FileCopyrightText: 2026 Miko Parkkinen
SPDX-License-Identifier: MIT
-->

# v0.4.1 launch and finalization record

**DRAFT — UNPUBLISHED**

## Candidate identity

- Package version: `0.4.1`
- Intended tag: `v0.4.1`
- Release commit/tree: `<fill-from-approved-final-main>`
- Canonical artifact manifest: `<fill-from-retained-build-once-manifest>`
- Publication lease/checkpoint: not started

The package is not tagged or published. The values above reserve no provider
identity and authorize no publication action.

## Owner-authorized publication commands

Run these only after the exact candidate qualification, mandatory package and
installed-product proof, fresh target observation, and owner publication
authorization all pass. The live multi-party custody demonstrations deferred by
the [v0.4.1 assurance scope](v0.4.1-assurance-scope.md) are not publication
preconditions for this release and must not be reported as passed:

```bash
release_version=0.4.1
release_tag=v0.4.1
release_commit=<approved-final-main-commit>

test "$(git rev-parse HEAD^{commit})" = "$release_commit"
test "$(python -c 'import metriplane; print(metriplane.__version__)')" = \
  "$release_version"
git diff --exit-code
git diff --cached --exit-code

# The existing release runbook and receipt/checkpoint-bound workflows own tag,
# TestPyPI, PyPI, and GitHub Release operations. Do not replace them with an
# ad-hoc upload or rebuild.
```

Exact workflow identifiers, artifact hashes, and receipt-bound dispatch values
remain blank until the approved candidate records produce them.

## Prepublication checklist

- [ ] Exact protected-main release candidate and tree recorded.
- [ ] Version `0.4.1` is consistent in package metadata and release copy.
- [ ] `v0.4.1` and registry target occupancy freshly observed.
- [ ] Canonical wheel, source distribution, and `SHA256SUMS` built once through
      the tag-independent staging path.
- [ ] Linux/macOS qualification and governed aggregate evidence pass on the
      exact candidate.
- [ ] Clean wheel and source-distribution installs, dependency checks, CLI,
      doctor, demo, and evidence-to-regression checks pass.
- [ ] MET-162 installed comparison proof passes against the retained published
      `0.4.0.post2` artifact and exact v0.4.1 candidate.
- [ ] `SOFTWARE_ASSURANCE` is `PASS` for the exact candidate.
- [x] `LIVE_MULTI_PARTY_PRODUCTION_CUSTODY` is explicitly
      `DEFERRED_BY_OWNER_TO_LATER_ASSURANCE_MILESTONE`; it is not `PASS`.
- [ ] Approval, two-store retention, CAS/lock/LKG, interruption, recovery, and
      conflict behavior remain green in the governed synthetic/integration suite.
- [ ] No conflicting publication operation, lease, or target state exists.
- [ ] Final release notes and commands match the retained candidate bytes.

## Postpublication steps

After the deliberately withheld publication transaction, observe every
declared target, compare exact retained bytes, and reconcile success or retain
the typed conflict/burn. Exercise the implemented success-chain, LKG,
pointer/index, and retention path under the v0.4.1 owner scope without claiming
independent production custody. Close MET-163 only after the applicable live
publication and reconciliation invariants are green.

No v0.4.1 DOI is planned. Never substitute the frozen v0.2.0 DOI
`10.5281/zenodo.20736619`.
