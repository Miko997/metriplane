<!--
SPDX-FileCopyrightText: 2025-2026 Miko Parkkinen
SPDX-License-Identifier: MIT
-->

# v0.3.0 launch materials

**DRAFT — UNPUBLISHED.** Do not post, upload, merge the website release, create
a GitHub release, change public repository metadata, or fill release identifiers
until production PyPI has been verified and the owner approves those actions.

## Values to fill from the actual release

- Final main commit: `<full SHA after release-candidate merge>`
- Annotated tag: `v0.3.0` (planned)
- Publication date: `<YYYY-MM-DD after publication>`
- GitHub release URL: `<created only after production verification>`
- Wheel filename and SHA-256: `<from workflow SHA256SUMS>`
- Source-distribution filename and SHA-256: `<from workflow SHA256SUMS>`
- Production PyPI verification run: `<workflow URL>`
- Website commit/deployment: `<fill after separate website approval>`
- v0.3.0 DOI: none unless a later, separate archive is intentionally created

Never place `10.5281/zenodo.20736619` in the v0.3.0 DOI field. It belongs to the
frozen v0.2.0 research artifact.

## Repository description

Proposed after publication:

> Open-source robotics incident replay and regression testing for bounded
> workcells. Turn recorded object state into an incident report, verified
> evidence, and a repeatable check.

## Repository topics

Proposed, subject to final owner review:

```text
robotics
robotics-testing
incident-analysis
deterministic-replay
regression-testing
reproducibility
workcells
industrial-automation
digital-twins
python
```

Do not add ROS, MCAP, rosbag, Isaac Sim, 3D, AI, machine control, safety, or
Windows topics for v0.3.0.

## Short technical announcement

> Metriplane v0.3.0 is available. Give it timestamped object positions and
> process rules; it replays the recorded workcell run and creates an incident
> timeline, Incident Report, verified evidence bundle, and repeatable regression
> check. The bundled camera-free demo finds a missing torque driver, a
> 35.0-second fastening delay, six events, and one incident.
>
> Try it:
>
> `python -m pip install "metriplane==0.3.0"`
>
> `metriplane demo --open`
>
> Linux/Ubuntu is fully tested on Python 3.12 and 3.13. The bundled camera-free
> demo is tested on macOS with both versions. Native Windows is not supported;
> WSL2 Ubuntu 24.04 has a bounded manual Python 3.12.3 validation for the
> installed-wheel camera-free and headless path. Automatic browser opening is
> not claimed there; users can omit `--open` or open the report path manually.
> Metriplane analyzes recorded state and does not control machinery or certify
> safety or quality.

Use “is available” only after a clean production-PyPI installation proves that
the published commands work. Before then, do not post this announcement; the
truthful status is “v0.3.0 is being prepared and is not published.”

## Demonstration asset plan

Prepare one short recording or screenshot from the exact final candidate. Do
not use a mock result or an older package while labeling it v0.3.0.

Capture:

1. the two installation/demo commands;
2. the scenario text naming the missing torque driver and 35.0-second delay;
3. six events and one incident;
4. evidence verification PASS;
5. regression check PASS;
6. the generated Incident Report heading and limits section.

Before publishing the asset:

- replace `<asset path>` with the reviewed file;
- record `<capture commit>` and confirm it is the final release commit;
- remove usernames, local absolute paths, terminal history, credentials, private
  photographs, and unrelated windows;
- add concise alt text;
- verify visible product casing is “Metriplane”;
- avoid implying robot control, physical accuracy, certification, or broader
  integration support.

Suggested alt text:

> Terminal and local Incident Report for the Metriplane bundled demo, showing a
> missing torque driver, a 35.0-second delay, six events, one incident, verified
> evidence, and a passing repeatable regression check.

## Final owner checklist

- [ ] Production PyPI installation is verified from a clean environment.
- [ ] Artifact filenames and SHA-256 values match the workflow manifest.
- [ ] GitHub release body is copied from the reviewed release-notes draft.
- [ ] Website pull request still has no fabricated hash, date, or DOI.
- [ ] Repository description, topics, and demonstration asset are reviewed.
- [ ] Frozen v0.2.0 evidence and DOI attribution remain unchanged.
- [ ] Zenodo automatic GitHub archiving is confirmed disabled.
- [ ] Owner explicitly approves creating the GitHub release and merging the
      website release pull request.

If Zenodo auto-archiving is enabled or uncertain, stop before creating the
GitHub release. A separate v0.3.0 archive, if wanted later, needs separate
metadata that does not inherit v0.2.0 research results.
