<!--
SPDX-FileCopyrightText: 2025-2026 Miko Parkkinen
SPDX-License-Identifier: MIT
-->

# v0.4.0.post2 launch and finalization record

## Verified release values

- Release commit: `98ad98c57198aaa78bbf8e9d94c699cd02c72da7`
- Release tree: `23178d0e9e9d571a198dc692b3d74fbbfe1eed2b`
- Annotated tag: `v0.4.0.post2`
- Annotated tag object: `b070c7563cae51d2889f2cea7fef3d6be79d5115`
- Production publication date: `2026-09-06`
- GitHub Release publication date: `2026-09-07`
- GitHub Release URL:
  <https://github.com/Miko997/metriplane/releases/tag/v0.4.0.post2>
- Wheel: `metriplane-0.4.0.post2-py3-none-any.whl`, SHA-256
  `2e206db012b16d2021ae98fc52a03a831d99746c4e4ff9a4bc7eefa5102a7f38`
- Source distribution: `metriplane-0.4.0.post2.tar.gz`, SHA-256
  `579d2578126fbf3cad07e20e365549cda2d17f49b4bb7b57695fca778f14c0ba`
- Manifest: `SHA256SUMS`, SHA-256
  `487f0fa8061d3fbb08654ce84bbab5d7dea4415b292f7e2714d45db9872ef30f`
- Actions artifact: `python-package-distributions`, ID `9992786913`
- Successful tag/TestPyPI workflow run: `34043220748`
- Production workflow run: `34045289769`; publication and production
  verification succeeded, while the original final reconciliation job failed
  and was later resolved through controlled broker recovery
- v0.4.0.post2 DOI: none

Never place `10.5281/zenodo.20736619` in the v0.4.0.post2 DOI field. It
identifies the frozen v0.2.0 research artifact.

## Published GitHub Release body

> Metriplane v0.4.0.post2 is the replacement publication identity for the
> reduced Truth Recovery core release. The immutable v0.4.0 attempt failed
> qualification before registry publication. Post1 then passed locked
> qualification and exact-artifact TestPyPI staging, but production stopped
> before lease creation or PyPI upload when GitHub exposed a narrow future-step
> status mismatch. The protected-main broker repair retired post1 unpublished;
> no post1 bytes reached production PyPI and none are reused for post2. Post2
> adds no product capability or assurance claim. The release includes
> deterministic governed inventories for the repository's functional and
> public surfaces, bounded portable external-fixture validation and evaluation,
> stronger generated-output path handling, and fail-closed protected-main and
> release-publication controls.
>
> Install the exact release:
>
> `python -m pip install "metriplane==0.4.0.post2"`
>
> `metriplane demo --open`
>
> To validate an already-normalized External Source Contract v1 bundle, use
> `metriplane external validate path/to/fixture`; evaluate it with
> `metriplane external run path/to/fixture --out external-run`.
>
> Canonical Linux qualification passed with `2,636 passed / 16 skipped`; hosted
> macOS qualification passed with `2,635 passed / 17 skipped`. Ruff, format,
> strict mypy, tracked compilation, strict documentation, generated currentness,
> independent wheel and source-distribution installs, and the complete
> installed-wheel evidence-to-regression proof all passed.
>
> Metriplane remains recorded, local, bounded, planar, and observe-only. The
> included source-specific fixtures do not establish generic robotics
> compatibility, physical accuracy, production safety, or completion of the
> v1.0 assurance programme. MP2-007, MP2-014, MP2-015, MP2-016, and MP2-017
> remain deferred to v0.4.1 Assurance Hardening.

The published body retained the exact limitations above and recorded the final
reconciliation incident accurately: production publication and verification
succeeded, the original workflow was not globally green, and the quarantined
lease was later released through a controlled provider-proof path.

## Prepared short announcement

> Metriplane v0.4.0.post2 is available: the replacement publication identity for the reduced Truth Recovery core release, for
> inspecting recorded workcell state, validating bounded external fixtures, and
> keeping the repository's functional surface reviewable. Install it with
> `python -m pip install "metriplane==0.4.0.post2"`, then run `metriplane demo --open`.
> It is observe-only software, not a production-safety, physical-accuracy, or
> generic robotics-compatibility claim. See the release notes for the exact
> qualified environments and deferred v0.4.1 assurance work.

No separate announcement was requested or published during this closure. The
GitHub Release and finalized release notes are the public release record.

## Build-once publication and readback

The annotated tag workflow is the only producer of the publication artifact
set. It runs the release gates and complete suite, builds exactly one wheel and
one source distribution, records `SHA256SUMS`, validates both distributions,
publishes the retained files to TestPyPI, and reads them back.

Production promotion is a separate owner-only dispatch from current protected
`main`. It names the successful tag run and exact version, acquires the
App-owned release lease, downloads the retained `python-package-distributions`
artifact, compares it with TestPyPI, and publishes those same bytes to PyPI. Do
not rebuild, rename, edit, or substitute any distribution between qualification,
TestPyPI, and production.

After publication, compare the production registry hashes with `SHA256SUMS`,
install `metriplane==0.4.0.post2` in a new clean environment outside the checkout, and
run package checking, version, doctor, and bounded product smoke. An ambiguous
upload or any hash disagreement is a release incident; do not retry by rebuilding.

## Final owner checklist

- [x] Version `0.4.0.post2` and tag `v0.4.0.post2` were unoccupied before candidate freeze.
- [x] The immutable `v0.4.0` tag still identifies
      `6a87936b5471c320efa6bcd7f5d1fe5569ca57b9`; no 0.4.0 registry package or
      GitHub Release exists, and no failed-workflow artifact was reused.
- [x] The immutable `v0.4.0.post1` tag still identifies
      `69f3d88c0779ff19962c455637a12701ff043876`; its retained artifacts and
      TestPyPI staging remain historical, production run `33963231781` remains
      cancelled before lease/upload, and no post1 bytes were reused.
- [x] Exact candidate head and tree are recorded; the broker-produced merged
      main tree equals that approved candidate tree, and the annotated tag
      points to the recorded merged-main commit.
- [x] Every mandatory candidate gate passed on that exact identity.
- [x] Release notes contain exact final qualification and supported-environment
      results with no remaining placeholders.
- [x] Tag/TestPyPI workflow completed successfully and retained exactly one
      wheel, one source distribution, and `SHA256SUMS`.
- [x] Production dispatch used the successful tag run ID, exact version, and
      exact confirmation phrase.
- [x] Production PyPI filenames and SHA-256 values match the retained manifest.
- [x] A clean production installation passed version, doctor, and bounded
      product smoke outside the source checkout.
- [x] The App-owned release lease was retired through the controlled recovery;
      the same App check is `completed/success/released`, the lease ref is
      absent, and protected-main health is green after publication.
- [x] Frozen v0.2.0 research evidence and DOI attribution remain unchanged.
- [x] Frozen source-specific v0.3.0 fixture/proof identities remain unchanged.
- [x] MP2-007, MP2-014, MP2-015, MP2-016, and MP2-017 remain explicitly
      deferred to v0.4.1.
- [x] Historical v2.5.x authority packets were neither consumed nor recreated.
- [x] No repository description, topics, website, or other discovery metadata
      were changed as part of this release closure.
- [x] Zenodo automatic archiving was positively disabled before GitHub Release
      creation: the credential-bearing inactive release hook was removed and
      provider readback found zero remaining Zenodo hooks.
- [x] The GitHub Release was created only after production byte readback and
      controlled lease reconciliation passed; no separate announcement was
      requested or published.

## Zenodo stop gate

Before GitHub Release creation, live provider inspection found the repository's
only Zenodo hook inactive. That credential-bearing hook was removed, exact-hook
readback returned not found, and the remaining Zenodo-hook inventory was empty.
GitHub therefore had no Zenodo release hook capable of automatically archiving
v0.4.0.post2. No v0.4.0.post2 DOI exists. A future archive requires separate
approval and truthful metadata; it must not inherit the v0.2.0 DOI or research
measurements.

## Post-publication repository reconciliation

This documentation-only follow-up records the immutable release identity,
retained hashes, public registry and GitHub Release readback, the original
fail-closed reconciliation incident, and its controlled recovery. It does not
alter, move, rebuild, or republish v0.4.0.post2 or its artifact set.
